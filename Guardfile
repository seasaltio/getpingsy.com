
guard :haml, run_at_start: true, input: 'app/templates', output: 'tmp/templates' do
  watch %r{^app/templates/.+(\.hbs\.haml)}
end


guard :haml, run_at_start: true, input: 'app/pages', output: 'public' do
  watch %r{^app/pages/.+(\.hbs\.haml)}
end

